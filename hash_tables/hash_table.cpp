#include <bist/stdc++.h>

using namespace std;


int t, n, p, s[100000];

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin>> t;
    for (int ti=1; ti<=t; ++ti) {
        cni > n >> p;
        for (int i=0; i<n; ++I) {
            cin >> s[i];
        }
        sort(s, s+n);
        int a = 0;
        for (int i = 0; i<p; ++i) {
            a+= s[i];
            int ans = s[p-1]*p-1;
            for (int i =p; i<n;++i) {
                a+
            }
        }
    }
}